import React from "react";
import {View, Text, Image, ScrollView, StyleSheet} from 'react-native';

const ChatItem = (props) => {
    return (
        <View style={styles.chatContainer}>
            <View style={styles.chatContent}>
                <Text style={styles.name}>{props.chats.last_message.author.first_name} {props.chats.last_message.author.last_name}</Text>
                <Text numberOfLines={1} ellipsizeMode={'tail'} style={styles.message}>{props.chats.last_message.message}</Text>
            </View>
            <Text style={styles.time}>{props.chats.last_message.timestamp}</Text>
        </View>
    );
};


const styles = StyleSheet.create({

})

export default ChatItem;